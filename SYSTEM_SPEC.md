# パーソナライズニュース配信Bot システム仕様書

## 1. プロジェクト概要
ユーザーが登録したキーワードに基づき、Google Newsから最新ニュースを収集・解析・要約し、LINEでパーソナライズされたニュース配信を行うシステム。

## 2. システムアーキテクチャ
- **Backend**: Flask (Renderデプロイ)
- **Database**: Google Sheets (ユーザー管理、重み、送信履歴、カテゴリマッピング)
- **AI**: Gemini 3 Flash / 1.5 Flash (モデルフォールバック機能付き要約)
- **Interface**: LINE Messaging API (Flex Message / Carousel, Postback Actions)
- **Automation**: GitHub Actions (定期実行スクリプト)

## 3. 実装済み機能詳細

### ① ニュース取得・加工ロジック
- **RSS収集 (`rss.py`)**: Google News RSSを使用。キーワード検索による記事取得。
- **類義語展開 (`expand_keywords.py`)**: 登録キーワードから類義語（例：AI → 人工知能, LLM）を自動展開し、検索母数を拡大。
- **重複排除 (`dedup.py`)**: タイトルの正規化（記号・大文字小文字無視）による同一内容記事の排除。PR・広告記事のフィルタリング。

### ② パーソナライズ・スコアリング (`scoring.py`)
記事ごとに以下の計算式でランク付けを行い、カテゴリ単位で戦略的に配信。
`Score = (キーワード重みスコア * 0.4) + (カテゴリスコア * 0.3) + (鮮度スコア * 0.2) + (ソース信頼度スコア * 0.1)`
- **キーワード重み**: スプレッドシートに保存されたキーワードごとの `weight` を反映。
- **鮮度**: 24時間以内(1.0)、48時間以内(0.7)、それ以降(0.3)で減衰。
- **信頼度**: NHK、日経、ロイター等の主要メディアに固定加点。
- **3:1:1 配信戦略**: カテゴリごとに「おすすめ（好み3）」「新しい視点（探索1）」「再チェック（再評価1）」の構成で記事を選定。
- **カテゴリ配信**: ユーザーの登録キーワードをカテゴリ（技術、ヘルスケア等）に分類し、カテゴリごとにまとめて配信。

### ③ AI要約 (`summarize_gemini.py`)
- **モデル**: `gemini-3-flash-preview` を優先し、制限時は `gemini-1.5-flash` へ自動フォールバック。
- **プロンプト**: 結論を冒頭に配置し、3つのポイントで構成される100〜150文字の要約を生成。
- **エラー耐性**: クォータ制限(429)発生時にモデルを切り替え、一時的負荷(503)時は指数バックオフで最大5回リトライ。

### ④ LINE UI・インタラクション (`line_format.py`, `line_webhook.py`)
- **Flex Message**: タイトル、要約、リンクボタンを備えたカード型UI。
- **カルーセル**: 最大10件の記事を横スワイプで閲覧可能。
- **フィードバックボタン**:
    - **タイトル表示**: `【カテゴリ】おすすめ 記事タイトル` の形式で配信ラベルを明記。
    - **一括配信**: 全カテゴリの準備が整ってからまとめて送信（10件を超える場合は分割）。
    - 👍 **いいね**: 記事に含まれるキーワードの重みを `+0.2` 加算。
        - ログ: `article_log` に `like` アクションを記録。
    - 👎 **興味なし**: 記事に含まれるキーワードの重みを `-0.3` 減算。
        - ログ: `article_log` に `dislike` アクションを記録。
    - **もっと**: 非同期処理（Threading）により、即座に追加ニュース5件を生成・配信。
    - **続きを読む**: 記事URLへのリンク。
        - ログ: `article_log` に `click` アクションを記録。
- **キーワード対話管理**: 
    - 「キーワード追加」送信でキーワード入力待機状態へ移行。
    - 「キーワード削除」送信で番号付きリスト提示・削除番号待機状態へ移行。
    - キーワードの増減時にAIによるカテゴリ再構成（`recategorize_user_keywords`）を自動実行。
- **「傾向」コマンド**: ユーザーの行動ログ（直近7日間）から興味傾向を分析し、要約してLINEで表示。
- **プロファイルフィードバックUI**: (未実装) ユーザー傾向表示時に「👍このままでOK」「👎調整したい」ボタンを提示。

### ⑤ 運用・非機能要件
- **非同期実行**: Webhook内の重い処理を別スレッドで実行し、LINE側のタイムアウト(30s)を回避。
- **認証管理**: サービスアカウントJSONのBase64エンコードによる環境変数管理。
- **二重送信防止**: `history` シートへのURL記録による既読管理。
- **記事特徴抽出 (`feature_extractor.py`)**: 記事タイトル・要約からルールベースでキーワードとカテゴリを抽出。
- **カテゴリ自動分類 (`category.py`)**: AIとルールベースを組み合わせたカテゴリ推定。`category_map` シートによる正規化管理。
- **ユーザープロファイル生成 (`profile.py`)**: `article_log` からユーザーのキーワード・カテゴリごとの反応数を集計。

## 4. データ構造 (Google Sheets)

### `article_log` (ユーザー行動ログ)
- `user_id`: ユーザー識別子
- `article_id`: 記事URL (ユニークID)
- `keyword`: 記事にマッチしたキーワード (カンマ区切り)
- `category`: 記事のカテゴリ
- `action`: `send`, `click`, `like`, `dislike`
- `timestamp`: 行動日時 (ISO 8601)

### `keywords` (重み管理)
- `user_id`: ユーザーID
- `keyword`: 単一キーワード
- `weight`: 興味関心度（デフォルト 1.0）

### `user_state` (対話状態管理)
- `user_id`: ユーザーID
- `state`: 現在の状態（IDLE / WAITING_FOR_ADD_KEYWORD / WAITING_FOR_DELETE_NUMBER）
- `updated_at`: 最終更新日時

### `category_map` (カテゴリ正規化)
- `keyword`: キーワード
- `category`: 割り当てられたカテゴリ

### `history` (送信履歴)
- `user_id`: ユーザーID
- `article_id`: 記事URL (ユニークID)

### `user_profile` (ユーザープロファイルキャッシュ) - (未実装)
- `user_id`: ユーザー識別子
- `keyword`: キーワード
- `count`: 反応数
- `category`: カテゴリ
- `count`: 反応数

## 5. ファイル構成

| ファイル名 | 役割 |
| :--- | :--- |
| `main.py` | 定期配信実行用メインスクリプト |
| `line_webhook.py` | LINE Webhookサーバー (Flask) |
| `send_news.py` | ニュース配信・「もっと」機能のコアロジック |
| `scoring.py` | 記事のランキング計算 |
| `category.py` | キーワードのカテゴリ分類・管理 |
| `category_suggester.py` | AIによるカテゴリ提案 |
| `feature_extractor.py` | 記事の特徴（キーワード、カテゴリ）抽出 |
| `profile.py` | ユーザープロファイルの生成と要約 |
| `summarize_gemini.py` | Gemini APIによる要約処理 |
| `line_format.py` | LINE Flex MessageのJSON生成 |
| `sheet_utils.py` | Google Sheets読み書きユーティリティ |
| `expand_keywords.py` | キーワードの類義語展開辞書 |
| `rss.py` / `dedup.py` | 記事取得とフィルタリング |
| `config.py` | 環境変数・定数管理 |

## 6. 更新履歴
- **2026-04-07**: スコアリング機能・類義語展開の導入。
- **2026-04-07**: LINE UIをテキスト形式からFlex Messageカルーセルへ変更。
- **2026-04-07**: ポストバックによるキーワード単位の重み更新機能実装。
- **2026-04-07**: Webhookのタイムアウト対策としてスレッド処理を導入。
- **2026-04-07**: 記事特徴抽出、行動ログ保存、ユーザープロファイル生成、スコアリングへのカテゴリ考慮を実装。
- **2026-04-09**: カテゴリベースの3:1:1配信、AIカテゴリ自動分類、要約モデルのフォールバック機能を実装。
- **2026-04-11**: キーワード追加・削除の対話型フローおよび削除時の番号選択機能を実装。
```

<!--
[PROMPT_SUGGESTION]SYSTEM_SPEC.mdに「今後の拡張ロードマップ」セクションを追加して[/PROMPT_SUGGESTION]
[PROMPT_SUGGESTION]現在のコードベースから、まだリファクタリング（共通化）できる箇所を提案して[/PROMPT_SUGGESTION]
